Module _15
    Sub main()
        Dim a As Integer = 1
        Dim b As Integer = 5
        Dim c As Integer = 2
        Dim x As Single
        x = (b ^ 2 - 4 * a * c) / (2 * a)
        Console.Write(x & "")
        Console.ReadKey()

    End Sub
End Module
